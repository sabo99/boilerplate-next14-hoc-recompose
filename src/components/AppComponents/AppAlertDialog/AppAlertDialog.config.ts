const config = {
  componentName: 'AppAlertDialog',
  defaultProps: {
    isOpen: true,
    title: 'Alert',
    message: 'Are you sure you want to proceed?',
    cancelText: 'Cancel',
    confirmText: 'Confirm'
  }
};

export default config;