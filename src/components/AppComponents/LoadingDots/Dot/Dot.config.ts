const COMPONENT_NAME = 'Dot';
const DOT_DIAMETER = 25;

const calculateMargin = (size: number) => (size * (3 / 10));

const config = {
  COMPONENT_NAME,
  DOT_DIAMETER,
  calculateMargin
};

export default config;
