const COMPONENT_NAME = 'LoadingDots';
const Dots = [
  { name: 'first', className: 'bg-primary-200 animate-dot-first' },
  { name: 'second', className: 'bg-primary-300 animate-dot-second' },
  { name: 'third', className: 'bg-primary-400 animate-dot-third' }
];

const config = {
  COMPONENT_NAME,
  Dots
};

export default config;